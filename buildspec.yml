version: 0.2
phases:
  install:
    runtime-versions:
      java: corretto21
  pre_build:
    commands:
      - echo "Starting CodeBuild phase"
      - mvn -version
      - docker --version
  build:
    commands:
      - mvn -B clean test package
artifacts:
  files:
    - target/*.jar
